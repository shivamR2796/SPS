@model SPS.Models.User_Id

@{
    ViewData["Title"] = "User_Register";
}
@{
    Layout = "";
}

@* <h1>user_Create</h1>

<h4>User_Id</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="User_Register">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Email" class="control-label"></label>
                <input asp-for="Email" class="form-control" />
                <span asp-validation-for="Email" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Password" class="control-label"></label>
                <input asp-for="Password" class="form-control" type="password"/>
                <span asp-validation-for="Password" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="C_Password" class="control-label"></label>
                <input asp-for="C_Password" class="form-control" type="password" />
                <span asp-validation-for="C_Password" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Register" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="User_Login">Back to List</a>
</div> *@

<style>
    body {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    background-image: url('https://images.unsplash.com/photo-1512386233331-f023884a92e8?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=1495&q=80');
    background-repeat: no-repeat;
    background-size: cover;
}

.overlay {
    position: absolute;
    width: 100vw;
    height: 100vh;
    background-color: rgba(0, 0, 0, 0.7);
    z-index: 1;
    display: none;
    backdrop-filter: blur(5px);
}

.logo {
    position:absolute;
    font-size:45px;
    color: #FFF;
    border-bottom: solid 1px #FFF;
    top: -20px;
    left: 40px;
    font-weight: bold;
    cursor: pointer;
}

.right-container {
    position: absolute;
    width: 60%;
    top: 20%;
    right: 0;
    display: flex;
    flex-direction: column;
    align-items: center;
}

.right-container h2 {
    font-family: Georgia, 'Times New Roman', Times, serif;
    font-size: 65px;
    text-align: center;
    font-weight: bold;
}

.right-container p {
    text-align: center;
    font-size: 45px;
    font-family: Arial, Helvetica, sans-serif;
    width: 60%;
    margin: 0;
    line-height: 1.2;
}

.center-container {
    width: 450px;
    height: 600px;
    background-color: #FFF;
    position: absolute;
    left: 50%;
    top: 50%;
    transform: translate(-50%, -50%);
    box-shadow: rgba(105, 108, 105, 0.3) 0px 5px 15px 0px;
    border-radius: 4px;
    display: none;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    z-index: 2;
    box-shadow: rgb(0,0,0) 0px 0px 15px 0px;
}

.center-container p {
    font-size: 28px;
    font-family: Arial, Helvetica, sans-serif;
    font-weight: bold;
}

form {
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
}

input {
    margin: 10px 0;
    padding: 10px;
    width: 400px;
    border-radius: 20px;
    border: 1px solid rgba(0, 0, 0, 0.2);
    font-size: 20px;
}

button {
    border: none;
    font-size: 24px;
    padding: 15px 120px;
    background-color: rgb(108, 151, 108);
    color: #FFF;
    cursor: pointer;
    margin-top: 50px;
    box-shadow: rgba(0, 0, 0, 0.25) 1.95px 1.95px 2.6px 0px;
}

button:hover {
    background-color: rgb(97, 135, 97);
}

.submitted-text {
    text-align: center;
    margin: 25px;
    display: none;
}
.welcome {
    color: green;
}

.submitted-text p {
    font-size: 24px;
    font-weight: normal;
}

.close-modal {
    margin-top: 10px;
}
media only screen and (max-width: 1000px) {
    body {
        background-size: 1200px 100vh;
    }

    .right-container {
        background-color: rgba(255, 255, 255, 0.8);
        backdrop-filter: blur(4px);
        height: 60%;
        width: 80%;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        border-radius: 4px;
        box-shadow: rgba(41, 41, 77, 0.25) 0px 13px 27px -5px, rgba(0, 0, 0, 0.3) 0px 8px 16px -8px;

    }

    .right-container h2 {
        font-size: 60px;
    }
    
    .right-container p {
        font-size: 40px;
        width: 70%;
    }

    .center-container {
        background-color: rgba(255, 255, 255, 0.9);
        backdrop-filter: blur(5px);
    }
}
</style>

    <div class="overlay"></div>
    <p class="logo">Herbal Tone</p>
    <div class="right-container">
        <h2>Herbology 101</h2>
        <p>Register today to receive the full course straight to your inbox.</p>
    <button asp-action="User_Register" class="modal-button">REGISTER</button>
    </div>
    <!-- FORM -->
    <div class="center-container">
        <form asp-action="User_Register">
            <p>Register</p>
            <input asp-for="Email" type="email" placeholder="email address">
            <span asp-validation-for="Email" class="text-danger" style="color:red"></span>
            <input asp-for="Password" type="password" placeholder="password">
        <span asp-validation-for="Password" class="text-danger" style="color:red"></span>
            <input asp-for="C_Password" type="password" placeholder="Confirm password">
            <span asp-validation-for="C_Password" class="text-danger" style="color:red"></span>
        </form>
        <button type="submit" class="submit-button">Register</button>
       
    </div>
    <!-- END FORM -->
    <!-- SUBMITTED -->

    <script>
    const registerBtn = document.querySelector('.modal-button');
    const submitBtn = document.querySelector('.submit-button');
    const closeBtn = document.querySelector('.close-modal');
    const container = document.querySelector('.center-container');
    const overlay = document.querySelector('.overlay');
    const form = document.querySelector('form');
    const name = document.getElementById('name');
    const submittedText = document.querySelector('.submitted-text');
    const span = document.querySelector('span');

    const submitComplete = () => {
        let storeName = '';

        if (name.value === '') {
            storeName = 'Tester';
        } else {
            storeName = name.value;
        }

        form.style.display = 'none';
        submitBtn.style.display = 'none';
        submittedText.style.display = 'block';
        span.innerText = storeName;
    }

    const closeModal = () => {
        overlay.style.display = 'none';
        container.style.display = 'none';
        submittedText.style.display = 'none';
        form.style.display = 'flex';
        submitBtn.style.display = 'block';
        name.value = '';
        storeName = '';
    }

    submitBtn.addEventListener('click', submitComplete)
    registerBtn.addEventListener('click', (e) => {
        e.preventDefault();
        overlay.style.display = 'block';
        container.style.display = 'flex';
    })

    </script>







@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
